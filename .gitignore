.claude/
.venv/
__pycache__/
*.pyc
.env.local
.env
documents.json
users.json
routing_slips.json
activity_log.json
saved_offices.json
doc_qr_tokens.json
client_qr_tokens.json
*.pyc
node_modules
tsconfig.tsbuildinfo
devices.json
README.md
settings.json
deped-document-tracker/
static/apk/doctracker.apk
static/apk/version.json
so_output/

# App-written database backups — these contain user password hashes and full
# document data. NEVER version-control them. The dir is kept via .gitkeep.
backups/pre-wipe/*.json
backups/**/*.json

# Backup round-trip test harness artifacts (captured backup JSON). Regenerated
# on every run; may contain seeded/real data — never version-control.
tests/_roundtrip_backup.json